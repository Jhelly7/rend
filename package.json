{
  "name": "streamvault-dispatcher",
  "version": "2.0.0",
  "description": "StreamVault Dispatcher — GitHub Actions round-robin trigger",
  "type": "module",
  "main": "dispatcher.js",
  "scripts": {
    "start": "node dispatcher.js",
    "dev": "node --watch dispatcher.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
